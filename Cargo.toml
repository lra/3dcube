[package]
name = "cube"
version = "0.2.0"
edition = "2024"

# rlib for the native bin, cdylib for the wasm build (see web/).
[lib]
crate-type = ["rlib", "cdylib"]

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
minifb = "0.28.0"
